<%= link_to gravatar_for(current_user, size: Settings.user.gravatar_size.size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("view_profile"), current_user %></span>
<span><%= t("count_micro", count: current_user.microposts.count) %></span>
